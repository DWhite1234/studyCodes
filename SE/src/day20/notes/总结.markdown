```
File
    -- getName()
    -- last。。。
    -- length()
    -- mdkDir()
    -- mdkDirs()
    -- lists()
    -- getAbsolutePath()
    ..............
IO
    节点流
        Reader  FileReder
        Writer  FileWriter
        InputStream FileInputStream
        OutputStream FileOutputStream
    处理流
        缓冲流
            缓冲字节流
                    BufferedInputStream   8192 byte[]
                    BufferedOutputStream
            缓冲字符流
                    BufferedReader        8192 char[]
                    BufferedWriter
        转换流
            字节流---->字符流
                InputStreamReader  将字节输入流--》字符输入流
                OutputStreamWriter 将字节输出流--》字符输出流

        数据流
            数据输入流
                DataInputStream
            数据输出流
                DataOutputStream
    
```
# 在文件复制中为什么不需要加append =true 的属性
```text
因为,在整个复制流程中,一次执行程序,每次都是追加,
在多次执行程序中是覆盖写
```