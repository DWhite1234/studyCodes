```shell
df	:空余硬盘
df -h:以人们较易阅读的 GBytes, MBytes, KBytes 等格式自行显示空余硬盘(主要是硬盘大小)
```

```shell
fdisk -l:显示所有硬盘的分区列表,只能在root用户下使用
Linux分区
    Device：分区序列
    Boot：引导
    Start：从X磁柱开始
    End：到Y磁柱结束
    Blocks：容量
    Id：分区类型ID
    System：分区类型

```

