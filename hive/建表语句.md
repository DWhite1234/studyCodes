```sql
-- EXTERNAL 指定为外部表
create EXTERNAL table tb1(
    id int
)
-- 指定行分隔符
row format delimited
--songsong,bingbing_lili,xiao song:18_xiaoxiao song:19,hui long guan_beijing_10010
--这一行数据而言 他的各字段分隔符是 ","
--对于分隔符是有默认值的 ascii 码表对应第二个  ^A   ctrl+v ctrl+a(linux)
fields terminated by ","

--bingbing_lili   xiao song:18_xiaoxiao song:19 hui long guan_beijing_10010
--对于这些集合各元素分隔符 "_"
--元素分隔符也有默认值 ascii 码表对应第三个  ^B     ctrl+v ctrl+b
collection items terminated by "_"

--xiao song:18   xiaoxiao song:19
--这个 map的kv分隔符 是":"
--map的kv分隔符也有默认值 ascii 码表对应第四个  ^C     ctrl+v ctrl+c
map keys terminated by ":"

--每一行数据的分隔符 '\n'
lines terminated by "\n"
-- 指定文件存储格式,默认是 textfile,一般不做修改
-- stored as textfile
--指定当前表的存储hdfs路径
location "hdfs路径"
tblproperties("k"="v");
```

<!-- 复制标结构与数据,但是使用默认分隔符^A -->
create table 表名 as select * from 表名
<!-- 只复制表结构,使用复制表的分隔符 -->
create table 表名 like 表名